POSTGRES_DSN=postgresql+asyncpg://qa:qa@localhost:55432/qa
REDIS_DSN=redis://localhost:56379/0
KAFKA_BOOTSTRAP=localhost:59092
SCHEMA_REGISTRY=http://localhost:58081

# All off = the service is correct, as far as we know.
# Each one arms one fault at runtime.
SCHEMA_DOC_MODE=false
SKIP_BEFORE_IMAGE=false
MIRROR_AFTER_COMMIT=false
CACHE_INCR=false
SEAL_ON_CLOCK=false

# Not one of the faults you are looking for -- the worked example.
DEMO_HEALTH_BUG=false
